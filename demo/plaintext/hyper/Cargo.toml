[package]
name = "laststack_plaintext_hyper"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = { version = "0.14", features = ["server", "tcp"] }
tokio = { version = "1", features = ["rt", "macros"] }
